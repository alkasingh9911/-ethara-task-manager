[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run db:migrate && npm run db:generate && node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
